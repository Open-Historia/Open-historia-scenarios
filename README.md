# Pax Historia — Scenario Hub

Community scenarios for [Pax Historia](https://github.com/Arkniem/pax-historia-2).
Every **issue** on this repo is a posted scenario. The game's **Community** tab reads this
repo directly — post here and your scenario shows up in-game for everyone.

## Post a scenario

1. In the game: **Scenarios → your scenario → Edit → Bundles → Download JSON**
   (or click **Publish to Hub** in the Community tab, which does steps 1–3 for you).
2. Open a [**new scenario post**](https://github.com/Arkniem/pax-historia-scenarios/issues/new?template=scenario.yml).
3. **Drag your exported `.json` into the description box** — GitHub uploads it and inserts
   a link. Without that link nobody can import your scenario.
4. Submit. Done — it now appears in the game's Community tab.

## Vote & discuss

- 👍 **react on an issue** to upvote it — the in-game *Most Liked* row uses these.
- 🚀 react if you played it.
- Comment on an issue to review or give feedback.

## Pinned & Official

- **📌 Pinned** posts carry the `pinned` label, which **only hub collaborators can apply** —
  GitHub silently drops labels set by anyone without write access (via the API, issue forms
  or URL parameters alike), so you cannot pin your own post. Great community scenarios get
  pinned by the moderators.
- **✓ Official** (purple in-game) marks posts whose author is the hub owner or a repo
  collaborator, as reported by GitHub's `author_association` — writing "official" in a
  title does nothing.

## Install counts

The official preset bundles live as assets on the
[**`bundles` release**](https://github.com/Arkniem/pax-historia-scenarios/releases/tag/bundles);
GitHub counts every download of those files, and the in-game **⬇ Most Installed** row shows
that count. (The `bundles/` folder in this repo is a mirror for older game versions and is
not counted.) Note: replacing a release asset resets its counter — install numbers count
since the bundle's last update.

## Import a scenario

In-game: **Community tab → Import** on any card. (Manually: download the attached `.json`
and use **Scenarios → Import JSON**.)

---

Scenarios are player-made content. The hub is moderated; broken or abusive posts are closed.
